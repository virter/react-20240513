/* eslint-disable react/jsx-key */

import styles from './styles.module.scss';

export const Footer = ({ }) => {
    return (
        <footer className={styles.root}>
            Footer
        </footer>
    );
};